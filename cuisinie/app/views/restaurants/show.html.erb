<p id="name">
    <%= @restaurant_hash["name"] %>
</p>
<p><%= @restaurant_hash["address"] %>
</p>
<img src="<%= @restaurant_hash["img_url"] %>" alt="img">
</p>
<p> Cost for two: <%= @restaurant_hash["cost"] %>*
</p>
<p class="user_id"><%= current_user.id%></p>
<p class="rest_id"><%= @hi %></p>
<div id="this">

</div>
<%= button_tag "Favorite" , :type=> 'button' ,:class=> 'favorite'%>

<h4>Comment list:</h4>


<% @comments.each do |comment| %>
<tb>
<p>
  <%= comment.content %>
  <%= form_for(comment, method: :delete, data: { confirm: "delete?" }) do |f| %>
    <%= f.submit "Delete"%>
    <% end %>
</p>
<% end %>

</tb>

<h3>Leave a comment:</h3>

<%= render "comments/comments_form", comment: @comment %>

<br>


<%= link_to "Back to Home", restaurant_path %>

<p id="lat"><%= @restaurant_hash["lat"] %>
</p>
<p id="lon"><%= @restaurant_hash["lon"] %>
</p>
<p><%= @restaurant_hash["restaurant_id"] %></p>
